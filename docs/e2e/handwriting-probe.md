# 손글씨 탐침 — 획이 그려지고, 견디고, 읽히는가

전제: 공통 전제와 같다 (자체 호스트 앱 · 로그인된 상태). 이 문서만의 전제가 둘 더 있다.

- ⚠ **이 흐름은 화면을 판정하지 않는다.** 판정 대상은 **능력의 경로**다 — 쓰기 학습형
  (구현 순서 13번)을 만들기 전에 *"손으로 쓴 획이 전달되고, 그려지고, 인식에 닿는가"* 를
  사람이 먼저 확인하는 자리다. 여기 통과가 「쓰기 화면이 되었다」를 뜻하지 않는다.
- ⚠ **이 자리에는 사용자 도달 경로가 0건이다.** 어느 코드도 이 화면을 push하지 않으므로
  앱을 띄우는 것만으로는 닿을 수 없다. 아래 「실행 준비」가 닿는 절차이고, **그 절차
  자체가 판정 항목 하나**다(E8).

**표 형식에 대하여.** 이 저장소의 흐름 문서는 두 형식을 쓴다 — ID·절차와 관찰 기준·결과
세 열짜리 표와, 항목 블록 아래의 `답하는 수단:` 불릿. **이 흐름은 항목마다 답하는 수단이
갈려서** (시뮬레이터로 답할 수 있는 항목이 하나도 없고, 일부는 요소 속성을 읽는 수단까지
따로 요구한다) 둘을 합쳐 `답하는 수단`을 표의 열로 세운다.

**「관찰」은 인상이 아니라 식별 가능한 요소로 적는다**(`README.md`). 아래 표가 부르는
이름은 화면이 실제로 다는 `data-testid`와 본문 문자열이다.

## 실행 준비 — 닿는 절차

**도달 경로가 0건이라 의도적인 한 줄 수정이 필요하다.** 이 수정은 커밋하지 않는다.

1. `apps/mobile/src/app/App.tsx`에서 `useReducer(navReducer, initialNav)`의 **둘째 인자**를
   `handwritingProbeNav`로 바꾼다. 그 이름을 `./navigation`에서 함께 import한다.
2. `pnpm --filter @libitums/mobile dev` — Debug 빌드는 dev 서버 번들을 읽는다.
3. 호스트 앱(Debug)을 실행한다. **여정 탭 자리에 탐침 화면이 뜬다** —
   `handwriting-probe-screen-title`의 본문이 `손글씨 탐침`이다.
4. 확인이 끝나면 **1번을 되돌린다.** `git diff -- apps/mobile/src/app/App.tsx`가 **0줄**인
   것이 되돌아왔다는 증거다. (E8)
5. **Release로 판정하려면** `pnpm --filter @libitums/mobile build` 뒤 Release 호스트로
   실행한다 — ⚠ **1번을 되돌리기 전에** 빌드해야 부팅 상태가 번들에 들어간다. 되돌림(E8)은
   그 빌드 뒤에 한다.

⚠ **Debug와 Release가 같은 답을 준다고 가정하지 않는다.** 아래 항목의 `답하는 수단` 칸이
빌드 종류를 요구하면 그 종류로 돈다. 공통 전제가 판정 환경을 Release로 두는 이유가
그대로 여기에도 걸린다.

## 실기에서만 답할 수 있는 항목 — E1~E8

⛔ **Q1~Q3을 시뮬레이터로 답하지 않는다.** 시뮬레이터는 연속 터치의 실제 전달을
재현하지 않고, 필기 인식 품질은 재현 대상 자체가 아니다. **「되는 것 같다」를 결과 칸에
적지 않는다** — 돌지 않았으면 `미실행`이다.

| ID | 단계 | 관찰(식별 가능한 요소) | 답하는 수단 | 결과 |
|---|---|---|---|---|
| E1 | **Q1-a 획이 보이는가.** 액션 행의 그리기 표면(`drawing-surface`)에 손가락으로 한 획을 천천히 긋는다 | 손가락을 따라 **검은 선이 표면 안에 보인다**. 보이지 않으면 `실패: 안 보임`으로 적고 **E3으로 간다** — 좌표가 안 온 것과 그려지지 않은 것이 거기서 갈린다. ⚠ **E2와 한 항목으로 묶지 않는다** | **실기 · 사람만** | 미실행 |
| E2 | **Q1-b 위치가 맞는가.** E1에서 선이 보였다면, 표면의 네 귀퉁이 가까이와 한가운데를 각각 찍어 선이 **손가락 아래**에 서는지 본다 | 선이 **손가락이 닿은 자리**에 선다. 어긋나면 어긋난 방향과 크기(대략 몇 배·몇 pt)를 적는다. ⭐ **이 항목이 필드 이름 정합을 판정한다** — 표면 구현이 터치 이벤트의 `x`·`y`를 **닿은 요소 기준 좌표**로 읽는데, **테스트 환경은 필드 이름을 검증하지 않아**(실어 준 대로 읽힌다) `ui` green이 실기 정합을 보증하지 않는다. 선이 **전혀 안 보이거나 늘 같은 자리(0,0 부근)** 면 실기가 `x`·`y`를 채우지 않는 것이고, 그때 후보는 `pageX`/`clientX` 또는 `detail`이다. ⚠ **이 답이 플랫폼을 가리키려면 표면의 CSS 박스와 `probeSurfaceSize`가 같은 수여야 한다** — 두 자리가 갈리면 `viewBox` 좌표 변환이 항등이 아니게 되어 **플랫폼과 무관한 이유로 같은 증상**이 나고, 그러면 이 항목의 답이 오염된다. 둘의 일치는 `DrawingSurface.ui.test.tsx`의 DS13이 지키므로, 러너가 초록이면 이 원인은 배제하고 읽는다 | **실기 · 사람만** | 미실행 |
| E3 | **Q1-c 핸들러가 불리는가.** 획을 하나 긋고 손가락을 뗀다. 계수 줄(`handwriting-probe-screen-counts`)의 본문 `획 <수> · 취소 <수>`를 본다 | 손가락을 뗄 때마다 **`획` 뒤 수가 1씩 는다**(속성으로는 `data-strokes`). ⇒ **늘면** 좌표는 왔고 **렌더가 문제**다. **안 늘었으면 같은 줄의 `취소` 뒤 수(`data-cancels`)를 먼저 본다** — 취소가 늘었으면 **바인딩은 닿은 것**이고 획이 중간에 끊긴 것이다(표면이 진행 중인 획 위에 새 `touchstart`가 오면 진행분을 **취소로 올린다** — 아래 M2). **둘 다 안 늘면** `<svg>`에 단 터치 바인딩이 실기에서 안 닿는 것이고, 다음 수단은 표면을 `<view>` 래퍼로 옮겨 좌표 기준을 다시 보는 것이다. **E1이 실패일 때 이 항목이 원인을 가른다** | **실기 · 사람만** | 미실행 |
| E4 | **Q1-d 스크롤과 다투는가.** 스크롤 영역(`handwriting-probe-screen-scroll`)을 위아래로 민다. 그다음 표면을 긋는다 | 스크롤을 밀면 **전역 목록만 움직이고 표면은 반응하지 않는다**(획 수가 늘지 않는다). 표면을 그으면 **목록이 움직이지 않는다**. ⚠ **이 관측은 보류 표 「제스처·커스텀 액션 축」 (1)의 답이 아니다** — 표면이 스크롤 **밖**(고정 액션 행)에 있어 **우회한** 구성이고, 그 행이 묻는 것은 스크롤 **안**의 우선순위다. 판정자는 여전히 `sentence-order.md`의 M3다 | **실기 · 사람만** | 미실행 |
| E5 | **Q3 Vision이 읽는가.** `지우기`(`handwriting-probe-screen-clear`)로 비운 뒤 **한글 한 글자**를 표면에 쓰고 `읽기`(`handwriting-probe-screen-read`)를 누른다 | 결과 줄(`handwriting-probe-screen-result`)의 **본문과 `data-status`를 그대로 적는다.** ⭐ **`빈 문자열`도 유효한 기록값이다** — `read`+빈 문자열 / `read`+틀린 글자 / `read`+맞는 글자 / `language-unsupported` / `failed` / `unavailable`이 **각각 다른 답**이고, 화면이 그 둘을 가르는 것은 **`data-status` 하나**다. ⚠ **본문이 비어 있을 때 눈만으로는 갈리지 않는다**(본문은 `read`일 때만 글자를 싣는다) — 요소 속성을 읽을 수단이 없으면 `본문 없음 · status 미확인`으로 적는다. **무엇을 썼는지 함께 적고 글자를 지어 적지 않는다.** ⭐ **「못 읽는다」로 나와도 이 항목은 실패가 아니다** — 이 탐침이 묻는 것이 그것이다 | **실기 · 사람만** (`data-status`를 가르려면 요소 속성 조회 수단이 함께 필요하다) | 미실행 |
| E6 | **Q3-b 흔들리는가.** 같은 글자를 여러 번, 그리고 다른 글자로도 E5를 반복한다 | 회차마다 **쓴 글자 · 본문 · `data-status`**를 줄로 적는다. 같은 글자에서 답이 갈리면 그 사실을 적는다 — 통과/실패가 아니라 **흔들림의 기록**이다 | **실기 · 사람만** | 미실행 |
| E7 | **JS 전역 셋 실기 확인.** 스크롤 영역을 끝까지 내려 `handwriting-probe-screen-globals` 안의 이름 목록을 눈으로 훑는다 | 이름이 **오름차순**으로 나열되고, 정적 근거가 못 박지 못한 것들을 여기서 확정한다 — 특히 **`TextEncoder`가 실제로 전역에 있는가**, 그리고 `canvas`·`OffscreenCanvas`·`ImageData`·`Blob`·`FileReader`·`btoa`/`atob`가 **없는 것이 맞는가**. 보이는 이름 중 그리기·이미지 인코딩·OCR에 쓸 수 있어 보이는 것이 있으면 **그 이름을 그대로 적는다**(ADR-0017 D1 조건 2를 다시 열어야 한다). ⚠ **화면이 찍는 것은 백그라운드 스레드의 `globalThis`다** — 메인 스레드(Element PAPI `__*`가 있는 쪽)와 **다른 셋**이고, 이 목록에 `__CreateElement` 계열이 없는 것이 정상이다 | **실기 또는 시뮬레이터** — 이 항목만은 시뮬레이터로도 답이 된다(터치도 인식도 걸리지 않는다) | 미실행 |
| E8 | **되돌림.** 「실행 준비」 4번을 수행한다 | `git diff -- apps/mobile/src/app/App.tsx`가 **0줄**이다. 0줄이 아니면 실패이고, 무엇이 남았는지 그대로 적는다 | 개발 기계(빌드 불필요) | 미실행 |

한 항목이라도 어긋나면 `실패: <관찰>`로 적는다 — 무엇이 어긋났는지, 어느 단계에서 처음
보였는지 그대로 적는다. **요약어(「확인함」·「잘 됨」)는 기록이 아니다.**

## 숫자로 적는 항목 — 통과/실패가 없다

**숫자를 적는다. "느리다"는 말은 순서를 정해주지 않지만 "열두 획"은 정해준다.**

**M1. `touchmove`마다 SVG를 다시 만들 때 몇 획까지 견디는가**

- 단계: `지우기`로 비운 뒤 획을 하나씩 늘려 가며 계속 긋는다. **획이 손가락을 따라오지
  못하는 느낌이 처음 드는 시점**에서 멈추고, 계수 줄의 `획 <수>`를 읽는다
- 관찰: **그 수를 적는다.** 프레임 수치가 아니라 **사람의 관찰**이다. 기기 모델 · iOS
  버전 · 빌드 종류(Debug/Release)를 **함께** 적는다 — 이 셋이 다르면 다른 숫자다
- 왜 적나: 표면이 `touchmove`마다 **끝난 획 전부 + 진행 중인 획**으로 `svg content`
  문자열을 새로 만든다. 좌표를 솎아내지 않는 것도, 획을 누적해 다시 그리는 것도 **일부러
  그렇게 둔 것**이다 — 먼저 재고 나서 고치려고. **이 숫자 없이 최적화를 넣지 않는다**
  (솎아냄·증분 렌더·표면 분할은 전부 이 수를 본 뒤의 수단이다)

**M2. 그리는 동안 취소가 몇 번 나는가**

- 단계: M1을 도는 동안 계수 줄의 `취소 <수>`를 함께 본다(속성으로는 `data-cancels`).
  M1이 끝나면 **손가락 둘을 얹는 시험을 따로** 한 번 돈다 — 한 손가락으로 긋는 도중에
  둘째 손가락을 표면에 얹고, 그때 수가 어떻게 움직이는지 본다
- 관찰: **최종 수와, 어떤 동작에서 늘었는지**를 적는다 — 화면 가장자리에서 그었을 때인지,
  손가락 둘을 얹었을 때인지, 천천히 그을 때인지. **동작을 안 적으면 이 숫자는 읽을 수 없다**
- ⚠ **이 수는 원인이 다른 둘을 함께 센다. 무엇을 하다 늘었는지가 그 둘을 가른다.**
  - **시스템이 제스처를 가져간 것** — 표면이 `touchcancel`을 받은 경우다. 한 손가락으로만
    긋다가 늘었으면 이쪽이고, **쓰기 화면의 배치를 정하는 신호가 이것**이다
  - **끼어듦** — 진행 중인 획 위에 새 `touchstart`가 온 경우다. 둘째 손가락을 얹었을 때가
    이쪽이고, **표면이 그것을 일부러 취소로 센다**(완성으로 올리면 긋다 만 조각이 글자에
    섞여 Q3의 인식 결과가 오염되고, 아무 수도 안 늘리면 E3을 읽는 사람이 그 소실을
    「바인딩이 안 닿는다」로 오독한다). ⇒ **손가락 둘을 얹었는데 수가 안 늘면 그것이 이상
    신호다** — 그 발화 자체가 표면에 안 닿고 있다는 뜻이고, E3으로 돌아간다
- 왜 적나: 앞의 갈래가 늘면 **시스템이 제스처를 가져가고 있는 것**이다. 쓰기 화면을 만들 때
  표면을 어디에 둘지(가장자리 여백 · 스크롤과의 거리)가 그 숫자에 붙어 있다

## 실행 기록

| 환경 | Host SHA-256 | bundle SHA-256 | E1–E4 | E5–E6 | E7 | E8 | M1 | M2 | 확인자 · 확인 시각 |
|---|---|---|---|---|---|---|---|---|---|
| — | 미기록 | 미기록 | 미실행 | 미실행 | 미실행 | 미실행 | 미기록 | 미기록 | 미기록 |

⚠ **이 흐름은 아직 한 번도 돌지 않았다.** 위 표의 모든 칸이 `미실행`/`미기록`이고,
**자동 unit/ui/integration 결과가 이 판정을 대신하지 않는다** — 그 계층들은 원리적으로
Q1~Q3을 못 본다(래스터화하지 않고, 좌표계가 전부 0이며, Vision이 없다).

⚠ **실기 판정은 사람이 따로 잡아야 한다.** 이 문서를 만든 작업은 실기에 접근하지 않았고,
`#44`(실기 TODO)에 이 흐름을 **등록하지 않았다** — 잊은 것이 아니라, **이 작업이 GitHub에
아무것도 올리지 않기로 해서** 등록하지 않았다. **등록은 사람이 한다.** 등록되지 않으면 이
파일은 돌지 않은 채로 남고, 그러면 쓰기 학습형의 능력 경로는 **확인된 것이 아니다.**
