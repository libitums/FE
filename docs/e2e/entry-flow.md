# 진입 흐름 여섯 화면 — 스플래시 · 온보딩 · 로그인 · 코드 검증 · 언어 선택 · 여정 입장 — iOS Release 수동 e2e

## 판정 채널과 범위

이 문서는 처음 앱을 켠 사용자가 스플래시 → 온보딩 → 로그인 → 코드 검증 → 언어 선택 →
여정 입장을 지나 여정 맵에 들어가는 흐름을, 그리고 임시 인증 토큰이 있을 때·없을 때
재실행이 어떻게 갈리는지를 iOS Release Host와 실기기에서 확인하는 수동 functional ·
Dynamic Type · 소프트 키보드 · VoiceOver e2e 절차다. 저장소 profile에는 `test.e2e`
명령과 runner가 없다 — **자동 E2E: not applicable(runner/command 없음)**이며, 이
계층의 「테스트를 쓰는 행위」는 이 흐름 문서를 세우는 것 자체다(LIB-247 §6.4(d)).
아래 표는 `.agent-harness/work/lib-261/spec.md` §12.1이 정본이고 이 문서가 사본이다 —
표 항목을 여기서 더하거나 빼지 않는다.

T1~T9·D1은 시뮬레이터에서 먼저 돌 수 있다. 그러나 그 결과만으로 `e2e-red`의 `moot`
값을 주장하지 않는다. **K1~K5는 실기가 답한다** — 소프트 키보드는 이 저장소에서
호스트에 처음 열리는 축이고, 시뮬레이터에는 실물 키보드 가림·완료 키 동작이 없다.
K5(코드 두 자리만 넣고 `확인`을 눌렀을 때의 반응)도 같은 이유로 실기 전용이다.
**V1~V3·V5는 iPhone 실기 · Release · VoiceOver로 사람만 판정한다**(K3·K4·V1·V5는
판정 절과 기록 절을 함께 지고, K5·V4·V6은 전부 관찰 기록형이다 — 아래 「소프트
키보드 여정 K1–K5」·「VoiceOver 여정 V1–V6」 참고) — Simulator나 정적/UI/integration
결과로 대체하지 않는다. K·V가 돌지 않으면 `e2e-implementation` → `e2e-green` →
`documentation` → `review`가 전부 서지 않는다(spec §12.3). 열리는 문은 **사람이
iPhone · Release · VoiceOver로 이 흐름을 도는 것**이고, 이 문서를 세운 세션에는
확인자도 실기도 없다.

⚠ **이 문서의 보정 r0.2**(spec **보정 r0.3**을 반영) — spec §12.1의 항목이
**18 → 21**로 늘었다(`accessibility` 지적 M-1·M-2·M-3·m-2 채택 — spec §0.10).
신설 셋은 **K5 · V5 · V6**이고, 형태가 바뀐 넷은 **K2**(오류 채널 후반부 추가) ·
**K3**(VoiceOver 기록 절 추가) · **K4**(기록 항목 → 판정 항목으로 승격) ·
**V4**(낭독 시간 기록 추가)다. 세는 자리는 `.agent-harness/work/lib-261/spec.md`
§12.1 표 하나이고, 이 문서는 그 사본이다.

**이벤트는 이 문서의 판정 항목이 아니다**(spec §7.5, 아래 「이벤트 운영 범위」).

정확한 제품 문자열과 testid는 다음을 정본으로 삼는다. 이 문서에 없는 이름이나 ID를
현장에서 새로 만들지 않는다.

- `apps/mobile/src/lib/entry-flow.ts`(화면 여섯의 순서 · 로그인 수단 넷 · 스플래시
  고정 시간 `1200ms`의 정본)
- `apps/mobile/src/lib/entry-language.ts`(언어 넷과 라벨의 정본)
- `apps/mobile/src/lib/auth-token.ts`(저장소 키 `libitum.auth.token`의 유일한 정본)
- `apps/mobile/src/screens/splash/splash.contract.ts` · `SplashScreen.tsx`
- `apps/mobile/src/screens/onboarding/onboarding.contract.ts` · `onboarding.ts` ·
  `OnboardingScreen.tsx`
- `apps/mobile/src/screens/login/login.contract.ts` · `login.ts` · `LoginScreen.tsx`
- `apps/mobile/src/screens/verification-code/verification-code.contract.ts` ·
  `verification-code.ts` · `VerificationCodeScreen.tsx`
- `apps/mobile/src/screens/language-select/language-select.contract.ts` ·
  `LanguageSelectScreen.tsx`
- `apps/mobile/src/screens/journey-entry/journey-entry.contract.ts` ·
  `JourneyEntryScreen.tsx`
- `apps/mobile/src/app/navigation.ts`(`Screen`의 진입 멤버 여섯 · `entryInitialNav` ·
  `entryScreenAfterLogin` · `isEntrySection`)
- `apps/mobile/src/app/App.tsx` · `app/index.tsx`

## 전제

공통 전제([docs/e2e/README.md](README.md)) + **Release Host** + ⭐ **앱을 삭제·재설치한
새 상태**(토큰이 없어야 진입 흐름이 선다. **지우는 수단이 앱에 없다** — 아래 「재설치가
유일한 재진입 수단이다」).

각 행의 결과에는 `통과` 또는 `실패: <관찰>`만 기록하고, 추정이나 인상은 넣지 않는다.
**관찰 기록형 자리는 결과 칸이 다르다** — `K5` 전체, `V4` 전체, `V6` 전체는
통과/실패를 가르지 않고 `기록: <관찰 그대로>`로 적는다(선례
`docs/e2e/notifications.md` V4). **`K3` · `K4` · `V1` · `V5`는 판정 절과 기록
절을 함께 진다** — 같은 칸에 `통과`/`실패: …`와 `기록: …`를 나란히 적는다(선례
`docs/e2e/settings.md` V1). `무음`도 유효한 기록값이고, 빈칸 · `미실행` ·
「확인함」 같은 요약어는 기록이 아니다. 아직 돌지 않은 행은 `미실행`, 아직 채우지
않은 증거는 `미기록`으로 둔다 — 결과 값이 아니라 결과가 아직 없다는 표시다(선례
[알림](notifications.md) · [설정](settings.md)).

### ⭐ 재설치가 유일한 재진입 수단이다 — 시작 전에 읽는다

저장소에 들어가는 항목은 **`libitum.auth.token` 하나뿐**이고(spec §0.3 D-d),
**그 토큰을 지우는 수단이 앱에 없다**(로그아웃은 이번 범위 밖 — spec §11.3). 한 번
T1~T7을 끝까지 돌아 토큰이 생기면, 그 뒤로는 앱을 재시작해도 **스플래시 뒤 바로
여정 맵**(T8 — R1)이라 **온보딩·로그인·코드 검증·언어 선택을 다시 볼 수 없다.**
이 표를 처음부터 다시 돌려야 하면 **기기에서 앱을 삭제하고 다시 설치**하는 것이
유일한 방법이다(T9 — R2). 이것은 결함이 아니라 이 회차의 **대가**다 — 확인자는
T1~T7 회차와 T8·T9 회차를 **서로 다른 설치**에서 준비해야 한다.

⚠ **수용 기준 5(「언어를 다시 켜면 사라진다」)는 이 흐름이 답하지 못한다.** 토큰이
있으면 언어 선택에 다시 갈 수 없고, 토큰을 지우려면 재설치인데 그러면 세션 자체가
새것이라 「언어를 골랐다가 다시 켰다」를 재현할 방법이 없다. **판정자는 integration
`IE12` 하나뿐**이다 — 이 표의 어느 행도 그 기준을 판정하지 않는다. 확인자는 T6에서
언어가 세션 안에서 유지되는 것만 보고, 「다시 켜면 사라진다」는 **찾지 않는다**.

## 실행 전 증거 고정

한 회차는 아래 값이 모두 채워진 **동일한 Release 산출물**로만 판정한다. 값이 비었거나
서로 다른 빌드에서 왔다면 T1을 시작하지 않는다. 이 흐름은 작성 시점까지 한 번도
실행된 적이 없으므로, 아래 표는 실행자가 회차마다 채운다. **T1~T7 회차와 T8·T9 회차는
설치 상태가 다르므로 행을 나눠 각각 채운다**(위 「재설치가 유일한 재진입 수단이다」).

| 증거 | 실행 회차 기록 |
|---|---|
| 소스 identity(커밋과 작업 트리 상태 또는 동등한 재현 식별자) | 미기록 — 실행 시 입력 |
| iOS Host.app SHA-256 | 미기록 — 실행 시 입력 |
| `main.lynx.bundle` SHA-256 | 미기록 — 실행 시 입력 |
| 실기기 모델 · iOS 버전 | 미기록 — 실행 시 입력 |
| 빌드 종류 | 미기록 — 실행 시 입력(Release여야 한다) |
| 앱 삭제·재설치 여부(이 회차의 시작 상태) | 미기록 — 실행 시 입력(T1 시작 전 필수 · T8은 재설치하지 않음 · T9는 재설치함) |
| 확인자 · 확인 시각(시간대 포함) | 미기록 — 실행 시 입력 |

## 기능 · Dynamic Type 여정 T1–T9 · D1

앱을 **삭제하고 새로 설치한** 상태에서 시작한다(토큰 없음). 아래 표는 spec §12.1
표의 사본이다 — ID · 단계 · 관찰 · 답하는 수단 열의 문구를 여기서 고치지 않는다.
`결과` 열만 이 문서가 더한다.

| ID | 단계 | 관찰(식별 가능한 요소) | 답하는 수단 | 결과 |
|---|---|---|---|---|
| T1 | 앱을 열고 기다린다 | `splash-screen-service-name` = `Duru`. 조작 수단이 **0건**. 고정 시간 뒤 **온보딩**이 선다. **바텀 네비게이션이 없다** | 시뮬레이터 또는 실기 | 미실행 |
| T2 | 온보딩에서 액션을 세 번 누른다 | 진행 점 셋 중 현재가 옮겨 가고 제목·본문이 바뀐다. 마지막 액션의 낱말이 갈린다(`다음` → `시작하기`). 끝나면 **로그인** | 시뮬레이터 또는 실기 | 미실행 |
| T3 | 전화번호를 입력하고 `전화번호로 계속하기` | 입력한 숫자가 칸에 보인다. **코드 검증**이 선다 | 시뮬레이터 또는 실기 | 미실행 |
| T4 | 코드를 3자리만 넣고 `확인` → 4자리를 채우고 `확인` | 3자리에서는 **아무 일도 일어나지 않는다**. 4자리에서 **언어 선택**이 선다 | 시뮬레이터 또는 실기 | 미실행 |
| T5 | 코드 검증에서 `로그인으로` | **로그인**이 선다(온보딩이 아니다) | 시뮬레이터 또는 실기 | 미실행 |
| T6 | 언어 선택에서 초기값이 **아닌** 항목을 누르고 `다음` | 누른 행에만 표식(`선택됨`)이 서고 다른 행의 표식이 사라진다. **여정 입장**에 **그 언어의 라벨**이 보인다 | 시뮬레이터 또는 실기 | 미실행 |
| T7 | `여정 시작하기` | **여정 맵**이 서고 **바텀 네비게이션이 그때 처음 보인다**. 탭 셋이 동작한다 | 시뮬레이터 또는 실기 | 미실행 |
| T8 | 앱을 완전히 종료하고 다시 연다 (**R1 — 토큰 있음**) | 스플래시 뒤 **바로 여정 맵**이다. 온보딩·로그인을 거치지 않는다 | 시뮬레이터 또는 실기 | 미실행 |
| T9 | 앱을 **삭제하고 재설치**한 뒤 열어 `Google로 계속하기`를 고른다 (**R2 — 토큰 없음**) | 스플래시 뒤 **온보딩**이다. 구글을 고르면 **코드 검증을 건너뛰고** 언어 선택이 선다 | 시뮬레이터 또는 실기 | 미실행 |
| D1 | Dynamic Type 기본 → 최대(AX5)에서 여섯 화면을 돈다 | 겹침 0 · 잘림 0. 액션 행과 나가기에 **닿을 수 있다**. 코드 검증 머리에서 나가기와 제목이 겹치지 않는다 | 시뮬레이터 또는 실기 | 미실행 |

**T8은 T1~T7과 같은 설치에서 이어 돈다(재설치하지 않는다) — 토큰이 있어야 하는
행이다.** **T9는 별도로 삭제·재설치한 설치에서 돈다** — T1~T8을 돈 설치를 그대로
쓰면 토큰이 남아 있어 이 행이 답하려는 「토큰 없는 재실행」을 재현하지 못한다. 한
항목이라도 어긋나면 `실패: <관찰>`로 적는다 — 무엇이 어긋났는지, 어느 단계에서 처음
보였는지 그대로 적는다. T · D 실패는 `e2e-red`의 `confirmed` 근거다(spec §12.3).

## 소프트 키보드 여정 K1–K5 — 실기

**이 저장소에서 소프트 키보드(입력기)가 호스트에 처음 열리는 자리다**(spec §11 —
`apps/mobile/src`에 그 전까지 `<input>`이 0건이었다). 시뮬레이터의 키보드는 실물
키보드의 가림·높이·완료 키 동작을 재현하지 않으므로 **아래 다섯은 실기로만 답한다.**

| ID | 단계 | 관찰(식별 가능한 요소) | 답하는 수단 | 결과 |
|---|---|---|---|---|
| K1 | 전화번호 칸을 탭한다 | 소프트 키보드가 **뜬다**. 종류가 **숫자 키패드**다(`purpose="telephone"`) | 실기 | 미실행 |
| K2 ⚠ **보정 r0.3** | 코드 칸을 탭하고 다섯 글자 이상, 그리고 숫자가 아닌 문자를 넣어 본다. **이어서 `확인`을 눌러 본다** | 칸에 **무엇이 남는지 그대로 적는다** — `1*23`처럼 남을 수 있다(`input-filter`를 안 넘기므로 네이티브가 거르는 것은 **길이뿐**이고 숫자 여부는 아무도 안 본다). **남은 채 `확인`을 누르면**: 오류 문구(§8)가 서면 **통과**, 아무 채널도 안 뜨고 무동작이면 `실패: <관찰>`. ⭐ **`default-value`가 실기에서 일회성인지(§0.10 (6))를 답하는 자리가 이 행이다** | 실기 | 미실행 |
| K3 | 키보드가 올라온 상태에서 코드 검증의 `확인`을 본다 | **고정 액션 행이 키보드에 덮이는가.** 덮이면 `실패: <덮인 높이·기기>` — ADR-0022 D1이 고치려던 것과 **같은 종류의 기능 손실**이다. 처방은 화면 CSS가 아니라 `ui-lynx`의 `avoid-keyboard` 개방이다(§15 Q-1). ⚠ **(기록 — 보정 r0.3)** VoiceOver를 켠 같은 상태에서 `확인`에 **닿는가 · 닿으면 활성화되는가**(`기록: <닿음/못 닿음 · 활성화됨/안 됨>`). **iOS 키보드는 별도 윈도라 덮인 요소가 접근성 트리에서 빠지지 않는다** ⇒ **시각은 막히는데 보조기술은 안 막히는 역방향 비대칭**이 생길 수 있다. 「VoiceOver로는 되더라」를 「안 덮인다」의 근거로 쓰지 못하게 **기록으로 남긴다** | 실기 | 미실행 |
| K4 ⚠ **보정 r0.3 — 판정 항목으로 승격** | 키보드를 내려 본다 | **(판정)** 내리는 수단이 **하나라도 있는가**(완료 키 · 바깥 탭 · 드래그). **하나도 없으면 `실패: 없음`이고 K3과 같은 차단 항목이 된다.** **(기록)** `기록: <실제로 통한 수단>`. ⭐ 승격 근거: `confirm-type`이 이 단위의 두 입력에 **안 붙고**(§11.2) 숫자 키패드에는 return 키가 없어 **「없음」이 나올 근거가 소스에 있다** — 기록형으로 두면 그것이 나와도 실패로 안 적힌다. K3(가림)과 곱해지면 **진행 자체가 막힌다** | 실기 | 미실행 |
| **K5** ⚠ **신설 r0.3** — 관찰 기록형 | 코드를 **두 자리만** 넣고 `확인`을 누른다 | **적는다** — 시각·소리·촉각 중 **무엇이라도 반응이 있었나**(`무음`·`무반응`도 유효한 기록값). 이어서 입력 칸에 VoiceOver 포커스를 주고 **들린 이름 전부**를 적는다 — 그것이 `<라벨>, <helper>, <최대>자 중 <현재>자 입력` 꼴이면 **왜 안 되는지가 그 채널로 전달된 것이다.** **판정 대상이 아니다** — `확인`에 `disabled` trait을 안 붙인 D10 결정의 **대가를 기록으로 남기는** 자리다(§4.3) | 실기 | 미실행 |

- **K1~K4는 통과/실패를 가르는 판정 항목이다.** K3·K4는 판정 절과 함께 **기록 절도
  진다** — 같은 칸에 `통과`/`실패: …`와 `기록: …`를 나란히 적는다(선례
  `docs/e2e/settings.md` V1). **K5는 관찰 기록형이다** — `기록: <관찰 그대로>`로
  적는다(예: `기록: 무음`, `기록: 진동 있음 · 오류 문구 없음`). `무음`·`무반응`도
  유효한 기록값이다.
- ⚠ **K3이 「덮인다」로 답하면 차단 항목이 된다**(spec §15 Q-1). 그 경우 화면 CSS를
  현장에서 고치지 않는다 — 처방 경로는 `@libitums/ui-lynx`의 키보드 회피 프롭 개방이고,
  그 프롭을 여는 것은 이 단위의 범위 밖이라 **사람(오케스트레이터)이 판단한다.**
  `실패: <덮인 높이 · 기기>`로만 적고 화면을 직접 고치지 않는다. **K3의 기록 절은
  그 판정을 바꾸지 않는다** — VoiceOver로 `확인`에 닿고 활성화된다는 관찰이 나와도
  그것이 「안 덮인다」의 근거가 되지 않는다(iOS 키보드는 별도 윈도라 덮인 요소가
  접근성 트리에서 빠지지 않기 때문이다 — `accessibility.md` (a)2).
- ⚠ **K4가 「없음」으로 답하면 `실패: 없음`이고 K3과 같은 차단 항목이 된다**(M-1,
  spec §12.1 · §12.3). `confirm-type`이 이 단위의 두 입력에 안 붙고(§11.2) 숫자
  키패드에는 return 키가 없어 「없음」이 나올 근거가 소스에 있다 — 그래서 기록형에서
  판정 항목으로 승격했다. K3(가림)과 K4(내릴 수단 없음)가 함께 「덮인다」·「없음」으로
  답하면 코드 검증 화면에서 **진행 자체가 막힌다.**
- K5는 **판정 대상이 아니다** — `확인`에 `disabled` trait을 붙이지 않은 D10 결정의
  대가를 기록으로만 남기는 자리다(m-2, spec §4.3 · §12.1).
- K1~K5가 돌지 않으면(실기가 없으면) `e2e-red`에 참인 값이 없다 — T · D만 돈 결과로
  `moot`를 주장하지 않는다(spec §12.3).

## VoiceOver 여정 V1–V6 — 실기 · 사람만

동일한 Release Host를 **실제 iPhone**에서 실행하고 VoiceOver를 켠다. **Simulator나
정적/UI/integration 테스트는 이 판정을 대체하지 않는다.** 자동 announcement나 임의
focus 이동은 기대하지 않는다 — 이 회차는 `announce`를 쓰지 않기로 했다(spec §4.4).

| ID | 단계 | 관찰(식별 가능한 요소) | 답하는 수단 | 결과 |
|---|---|---|---|---|
| V1 | VoiceOver로 로그인 화면을 훑는다 | **(판정)** 수단 넷이 각각 **정지 하나**이고 이름이 §8의 라벨과 같다. **(기록)** 전화번호 **입력 필드 위에서 들린 것 전부**(이름·값·편집 가능 여부) — 이 스택의 `<input>`에서 무엇이 들리는지 **아무도 모른다** | 실기 · 사람만 | 미실행 |
| V2 | VoiceOver로 언어 선택을 훑는다 | 선택된 행만 `<라벨>, 선택됨, 버튼`으로, 나머지는 `<라벨>, 버튼`으로 들린다. 표식 낱말이 **따로 정지하지 않는다** | 실기 · 사람만 | 미실행 |
| V3 | VoiceOver로 코드 검증 머리를 훑는다(**기본·최대 배율 모두**) | `로그인으로, 버튼` → `코드 검증, 머리말` 순. 역전되면 `실패: <배율·들린 순서>` | 실기 · 사람만 | 미실행 |
| V4 | VoiceOver를 켠 채 스플래시가 전이하는 순간을 관찰한다 | **적는다** — 화면이 갈릴 때 포커스가 어디로 갔고 무엇이 들렸나(`무음`도 유효값). **판정 대상이 아니다** — 이 회차는 `announce`를 쓰지 않기로 했고(§4.4), 이 기록이 `docs/adr/README.md` 보류 표 **「전환 통지」 행의 일곱째 사례**가 된다. ⚠ **(보정 r0.3)** 함께 적는다 — 스플래시의 **두 텍스트 중 낭독이 시작된 것이 있는가 · 끝난 것이 있는가**(`무음`·`시작만`·`끝남` 중 하나). §0.3 D-a의 대가가 실제로 얼마인지를 재는 자리다 | 실기 · 사람만 | 미실행 |
| **V5** ⚠ **신설 r0.3**(온보딩) — 판정 + 기록 | VoiceOver로 온보딩을 훑고, 액션을 눌러 다음 단계로 간 뒤 **다시 훑는다** | **(판정)** 정지가 `<제목>, 머리말` → 본문 → **진행 묶음** → `다음, 버튼`이고 진행 묶음에서 **`N단계 중 M단계`가 들린다**(§8). **(기록)** 「셋 중 몇 번째인가」를 아는 채널이 하나라도 들리는가(`무음`·`없음`도 유효값)와, 단계를 넘긴 뒤 **정지 수와 들린 것이 어떻게 갈리는지**. ⭐ **M-4를 실기로 잡는 자리**이고, 코드 칸 이름 갱신과 함께 이 회차의 새 이름을 처음 듣는 자리다 | 실기 · 사람만 | 미실행 |
| **V6** ⚠ **신설 r0.3**(여정 입장 + 탭 바 등장) — 관찰 기록형 | VoiceOver로 여정 입장을 훑고 `여정 시작하기`를 활성화한다 | **적는다** — 정지 목록(`여정 입장, 머리말` → 본문 → **언어 줄** → `여정 시작하기, 버튼`)과 **언어 줄에서 들린 것 그대로**(m-3 · **§15 Q-8이 그 기록으로 판단한다**). 이어서 활성화 **직후 처음 읽힌 것**과, 네 손가락으로 화면 아래쪽을 탭했을 때 **탭 셋에 닿는지**(`무음`도 유효값). **판정 대상이 아니다** — 이 회차는 `announce`를 쓰지 않기로 했다(§4.4). 이 기록이 보류 표 「전환 통지」의 **여덟째 사례**(조작 수단이 **생기는** 첫 사례)가 된다 | 실기 · 사람만 | 미실행 |

- **V1의 결과 칸은 둘로 나눠 적는다** — 판정 절은 `통과`/`실패: <들린 그대로>`,
  기록 절은 같은 칸 안에 `기록: <입력 필드 위에서 들린 것 전부>`를 덧붙인다(선례
  `docs/e2e/settings.md` V1).
- **V4는 관찰 기록형 — 통과/실패가 없다.** `기록: <화면이 갈릴 때 포커스 · 들린 것 ·
  낭독 시작/종료 여부>`로 적는다. `무음`도 유효한 기록값이다. ⚠ **(보정 r0.3)**
  스플래시 두 텍스트(`Duru` · `한국어를 여정으로 배웁니다`) 중 **낭독이 시작된 것이
  있는가 · 끝난 것이 있는가**(`무음`·`시작만`·`끝남` 중 하나)를 함께 적는다 —
  spec §0.3 D-a가 고정한 1200ms 대가가 실제로 얼마인지 재는 자리다(M-3).
- **V5의 결과 칸도 둘로 나눠 적는다** — 판정 절은 `통과`/`실패: <들린 그대로>`,
  기록 절은 같은 칸 안에 `기록: <「셋 중 몇 번째인가」를 아는 채널 · 단계 이동 전후
  정지 수 차이>`를 덧붙인다(V1과 같은 형식). ⚠ **신설 r0.3** — 온보딩은 화면 여섯 중
  이 회차 전까지 **보조기술로 한 번도 훑지 않은** 화면이었다(M-2, `accessibility.md`
  R-8). `OB-U7`이 진행 래퍼에 `${총}단계 중 ${현재}단계` 이름을 건 것을 실기가
  처음 확인하는 자리이기도 하다.
- **V6은 관찰 기록형 — 통과/실패가 없다.** `기록: <정지 목록 · 언어 줄에서 들린 것 ·
  활성화 직후 읽힌 것 · 탭 셋 도달 여부>`로 적는다. ⚠ **신설 r0.3** — 여정 입장도
  이 회차 전까지 보조기술로 한 번도 훑지 않은 화면이었다(M-2, `accessibility.md`
  R-9). 언어 줄이 `한국어` 한 낱말로만 들리는지의 기록은 **§15 Q-8**(m-3, 보이는
  줄을 더할지)의 판단 자료가 된다 — 이 문서는 그 판단을 내리지 않는다.

### V3의 배율을 산술로 닫을 수 없는 이유

코드 검증 머리는 ADR-0023 게이트 A 실패(Δ 12) → **안 B**로 갚은 형태이고, **같은
레시피에서 산술이 통과인데 실기가 뒤집은 사건이 이미 있다**(LIB-227 **E-S3**,
`docs/e2e/journey-map.md`). VoiceOver는 DOM이 아니라 프레임 위치로 순회하고 동률 ·
근접의 타이브레이크가 이 저장소에서 확인된 적이 없다. ⇒ **배율을 박는 것은 이 행에서만
답이 나오는 축이다**(선례 `docs/e2e/notifications.md` V1의 「기본 · 최대 배율 모두」).
**어긋나면 CSS를 현장에서 추측으로 고치지 않는다** — 대응 경로는 ADR-0023의 대체안
B′다.

### V4 · V6의 지위 — 판정 대상이 아니다

이 관측이 드러낼 것을 고칠 경로가 이 회차에 없다(announce를 쓰지 않는 것이 이
회차의 결정이다 — spec §4.4). 그래서 실패 조건을 두지 않는다(선례
`docs/e2e/notifications.md` V4). **V6도 같은 이유로 판정 대상이 아니다** — 여정
입장·탭 바 등장 순간의 낭독도 `announce`를 쓰지 않는 이 회차의 결정 아래에서는
고칠 경로가 없다.

### e2e-red 판정 항목 셈 — 21항목이 어떻게 갈리는가(spec §12.3, 보정 r0.3)

⚠ **보정 r0.3 — K4가 기록 항목에서 판정 항목으로 옮겨 갔다(M-1). K5·V5·V6이
신설되며 판정 항목과 기록 전용 항목의 셈이 달라졌다.**

- **판정 항목(18개)** — 실패 하나로 `confirmed`가 서는 자리: `T1~T9`(9) ·
  `D1`(1) · `K1~K4`(4) · `V1~V3`(3) · **`V5`의 판정 절**(1).
- **기록 절(판정 항목에 곁달린 넷)** — `K3` · `K4` · `V1` · `V5`의 기록 절. 판정을
  바꾸지 않지만 `moot`가 서려면 비어 있으면 안 된다.
- **기록 전용 항목(셋)** — `K5` · `V4` · `V6`. 통과/실패가 없고 `기록: …`만 있다.
  같은 이유로 `moot`가 서려면 비어 있으면 안 된다.

`e2e-red`의 `test.e2e.red-proof: moot`는 **판정 항목 18개 전부 통과** +
**기록 절 넷(K3·K4·V1·V5)과 기록 전용 셋(K5·V4·V6)이 전부 완비**됐을 때만 선다.
판정 항목 중 하나라도 실패하면 `confirmed`(실패 ID와 관찰을 적는다). **K3이
「덮인다」로 실패하면**(위 소절) 이 계약의 커밋 범위 밖 처방(`ui-lynx` 프롭 개방)이
필요한 차단 항목이 된다는 것도 여전히 참이다. K · V를 돌릴 실기가 없으면 어느 값도
기록하지 않는다(spec §12.3 「기록하지 않는다」 행) — T · D만 돈 결과로 `moot`를
주장하지 않는다. 열리는 문은 **사람(사용자)이 iPhone · Release · VoiceOver로 이
흐름을 도는 것**뿐이다.

## 실행 기록

| 환경 | Host SHA-256 | bundle SHA-256 | T1–T7 | T8(재설치 안 함) | T9(재설치함) | D1 | K1–K5 | V1–V6 | 확인자 · 확인 시각 |
|---|---|---|---|---|---|---|---|---|---|
| iPhone 실기 · iOS 미기록 · Release | 미기록 | 미기록 | 미실행 | 미실행 | 미실행 | 미실행 | 미실행 | 미실행 | 미기록 — 실행 시 입력 |
| iOS Simulator · iOS 미기록 · Release | 미기록 | 미기록 | 미실행 | 미실행 | 미실행 | 미실행 | 미실행 — Simulator로 대체 불가 | 미실행 — Simulator로 대체 불가 | 미기록 — 실행 시 입력 |

이 흐름은 작성 시점까지 실행된 회차가 없다. 회차가 생기면 해당 환경 행의
`미실행` · `미기록`을 그 회차의 값으로 바꾸거나 행을 추가하고, 「실행 전 증거 고정」
표를 그 회차의 값으로 채운다.

## 이벤트 운영 범위

`EntryAppProps.entryEventSink`의 현재 제품 진입점은 명시적 `null`이다(spec §7.5).
따라서 이 문서의 어떤 관찰도 이벤트(`entry_screen_viewed` · `entry_login_method_selected` ·
`entry_completed`)가 실제 운영 집계로 전송되었다는 증거로 쓰지 않는다. 이벤트의 경계와
순서는 `integration`(`App.entry.integration.test.tsx`)이 진다. 운영 sink 연결과 서버
집계는 별도 후속 범위다.
