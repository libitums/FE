import type { Preview } from "storybook-lynx-rsbuild";

const preview: Preview = {
  parameters: {
    controls: {
      expanded: true,
    },
    layout: "centered",
  },
};

export default preview;
