// LIB-221 (ui-scaffold): 계약(scratchpad/lib221/contracts/screens.contract.ts)이
// 고정한 시그니처만 만든다. 동작은 다음 단계(ui)가 채운다.

import type { ReactNode } from "@lynx-js/react";

export function JourneyMapScreen(): ReactNode {
  return <view />;
}
